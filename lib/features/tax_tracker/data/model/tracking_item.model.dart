class TrackingItem {
  String title;
  String authority;
  String date;
  String uploadedBy;
  bool isCompleted;
  String url;

  TrackingItem({
    required this.title,
    required this.authority,
    required this.date,
    required this.uploadedBy,
    required this.isCompleted,
    required this.url,
  });
}
